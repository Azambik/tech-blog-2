<div class="comments">
  {{#each this}}
  <section class="comment  border-coco p-0">
    <div class="bg-brown">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text bg-pistachio wrap">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>